# Proposal-only SQL

Files here are design review artifacts, not applied migrations. They must be converted with `supabase migration new`, tested on a staging branch, reviewed for mobile compatibility, and explicitly approved before application.
